## fastify-serverless-boilerplate
- Setup serverless-aws profile
  > serverless config credentials --provider aws --key ############### --secret ############################## 
- Click on `debugger > run` in VsCode.
